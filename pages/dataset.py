import streamlit as st
import pandas as pd
from PIL import Image
import os

def render():
    st.title("Data Exploration")
    st.markdown("""
    Deepfake detection requires a clear understanding of the dataset before developing Machine Learning (ML) 
    and Deep Learning (DL) models.  
    This section explores the dataset structure, distribution, and early differences between real and fake images.
    """)

    # ============================
    # 3 SUBTABS (horizontal)
    # ============================
    tab1, tab2, tab3 = st.tabs([
        "Dataset Overview",
        "Distribution Analysis",
        "Feature Analysis (Real vs Fake)"
    ])

    # ---------------------------------------------------------
    # TAB 1 — DATASET OVERVIEW
    # ---------------------------------------------------------
    with tab1:
        st.subheader("Part 1 – Dataset Overview")
        
        st.markdown("""
        This project uses the public dataset **“140K Real and Fake Faces”** by Xhlulu on Kaggle.  Source: [Kaggle Dataset](https://www.kaggle.com/datasets/xhlulu/140k-real-and-fake-faces)
        """)
        
        # ---- a) Dataset Overview ----
        st.markdown("#### Dataset Overview")
        st.markdown("""
        The dataset contains **140,000 face images**, evenly split into two groups:
        - **Real** – real images of human faces.
        - **Fake** – synthetic images generated by a **GAN (StyleGAN v2)** using *ThisPersonDoesNotExist.com*.
        
        Each image is a single, centered, high-quality face, suitable for training and evaluating deepfake detection models.
        """)
        
        # ---- b) Dataset Features ----
        st.markdown("#### Dataset Features")
        data_features = {
            "Attribute": ["Total Images", "Image Size", "File Format", "Number of Classes", "Source of Fake Images", "Compressed Size"],
            "Description": ["140,000 images", "256 × 256 pixels", "JPEG (.jpg)", "2 classes: Real and Fake", "GAN (StyleGAN v2)", "~2.1 GB"]
        }
        df_features = pd.DataFrame(data_features)
        st.table(df_features)
        
        st.markdown("""
        Images are standardized in size and layout, allowing models to focus on detecting **micro-artifacts** rather than image preprocessing.
        """)

        # ---- Detailed Dataset Attributes ----
        st.markdown("#### Detailed Dataset Attributes")
        detailed_features = {
            "Attribute": ["File Format", "Image Size", "Color Channels", "Number of Classes", "Total Images", "Compressed Size"],
            "Description": ["JPEG (.jpg)", "256 × 256 pixels", "RGB (3 channels)", "2 classes: Real and Fake", "Approximately 140,000 images", "4.04 GB"]
        }
        df_detailed = pd.DataFrame(detailed_features)
        st.table(df_detailed)
        
        st.markdown("""
        Fake images are generated using **StyleGAN**, often exhibiting artifacts around eyes, hair, background blur, or lighting inconsistencies, which models can learn to distinguish from real images.
        """)
        
        # # ---- References ----
        # st.markdown("<h2 id='references' class='fade-in-section' style='color:#191970;'>References</h2>", unsafe_allow_html=True)
        # st.markdown("""
        # <div class="ref-box" style="background-color:#e6f2ff; padding:15px; border-radius:10px;">
        #     <ul>
        #         <li>Xhlulu. (2021). 140K Real and Fake Faces. <i>Kaggle Dataset</i>. <a href="https://www.kaggle.com/datasets/xhlulu/140k-real-and-fake-faces" target="_blank">140K Real and Fake Faces</a></li>
        #     </ul>
        # </div>
        # """, unsafe_allow_html=True)

    # ---------------------------------------------------------
    # TAB 2 — DISTRIBUTION ANALYSIS
    # ---------------------------------------------------------
    with tab2:
        
        # MAIN SECTION TITLE
        st.subheader("Part 2 – Distribution Analysis")

        st.markdown("""
        Understanding how real and fake images are distributed across the dataset splits 
        (train, validation, test) is essential for balanced learning and reliable model evaluation.
        """)
        
        root = os.path.dirname(os.path.dirname(__file__))  

        img_path_1 = os.path.join(root, "imgs", "classDistribution.png")
        img1 = Image.open(img_path_1)
        
        img_path_2 = os.path.join(root, "imgs", "imageStatistics.png")
        img2 = Image.open(img_path_2)
        
        img_path_3 = os.path.join(root, "imgs", "colorChannel.png")
        img3 = Image.open(img_path_3)
        
        # ================
        # 1. CLASS DISTRIBUTION
        # ================
        st.markdown("#### Class Distribution Across Splits")

        col1, col2 = st.columns([1, 1])
 
        with col1:
            st.image(img1, use_column_width=True, caption="Class Distribution Across Splits")

        with col2:
            st.markdown(
            """
            This chart presents the distribution of real and fake images across the training, validation, 
            and test sets. 
            
            All dataset splits maintain an equal number of samples for both classes, 
            ensuring a perfectly balanced 50/50 ratio at every stage of the modelling pipeline.

            A balanced dataset is critical for building reliable detection systems, as it prevents the model 
            from learning biased patterns or overfitting to a dominant class.  
            
            This balanced structure supports fair evaluation, stable training behaviour, and consistent 
            generalisation performance across different model architectures.
            """,
            unsafe_allow_html=True)

        st.markdown("---")

        # ================
        # 2. BASIC IMAGE STATISTICS
        # ================
        st.markdown("#### Basic Image Statistics")

        col3, col4 = st.columns([1, 1])

        with col3:
            st.markdown("""
            This chart provides a comparison of key pixel-level statistics between real and fake images, 
            including mean intensity, pixel variance, brightness, and contrast.  
            
            The values between the two classes are closely aligned, indicating good dataset quality and 
            consistent preprocessing across all samples.

            Despite the similarities, real images demonstrate slightly higher contrast and statistical variation, 
            reflecting natural camera sensor noise and real-world texture complexity.  
            
            In contrast, fake images generated by GAN models tend to appear smoother and more uniform, 
            showing reduced pixel-level variation.  
            These subtle differences can help machine learning models distinguish between authentic and synthetic content.
            """)

        with col4:
            st.image(img2, use_column_width=True, caption="Basic Image Statistics")

        st.markdown("---")

        # ================
        # 3. RGB CHANNEL BALANCE
        # ================
        st.markdown("#### RGB Channel Balance")

        col5, col6 = st.columns([1, 1])

        with col5:
            st.image(img3, use_column_width=True, caption="RGB Channel Balance")

        with col6:
            st.markdown("""
            This chart illustrates the average pixel intensities across the Red, Green, and Blue channels for both real 
            and fake images. 
            
            The overall color distribution is consistent across classes, suggesting balanced dataset 
            construction and minimal preprocessing bias.

            While the differences are not large, slight variations in RGB channel levels—especially in the red and 
            green channels—may indicate generative inconsistencies typical of GAN-produced images.  
            
            These subtle deviations in color reproduction may reveal imperfections in synthetic image generation, 
            providing useful cues for classification models during training.
            """)

        st.markdown("---")

    # ---------------------------------------------------------
    # TAB 3 — FEATURE ANALYSIS
    # ---------------------------------------------------------
    with tab3:
        st.subheader("Part 3 – Real vs Fake Feature Analysis")
        
        img_path_4 = os.path.join(root, "imgs", "PRNU_real.png")
        img4 = Image.open(img_path_4)
        img_path_5 = os.path.join(root, "imgs", "PRNU_fake.png")
        img5 = Image.open(img_path_5)
        
        img_path_6 = os.path.join(root, "imgs", "LBP_real.png")
        img6 = Image.open(img_path_6)
        img_path_7 = os.path.join(root, "imgs", "LBP_fake.png")
        img7 = Image.open(img_path_7)

        img_path_8 = os.path.join(root, "imgs", "DCT_real.png")
        img8 = Image.open(img_path_8)
        img_path_9 = os.path.join(root, "imgs", "DCT_fake.png")
        img9 = Image.open(img_path_9)
        
        img_path_10 = os.path.join(root, "imgs", "FFT_real.png")
        img10 = Image.open(img_path_10)
        img_path_11 = os.path.join(root, "imgs", "FFT_fake.png")
        img11 = Image.open(img_path_11)

        st.markdown("""
        #### PRNU Analysis 
        """)
        st.markdown("""
        Photo-Response Non-Uniformity (PRNU) represents the unique sensor noise pattern produced by a real camera sensor.  
        The visualizations below compare **real vs fake PRNU maps**, helping reveal structural differences between 
        authentic and GAN-generated images.
        """)

        # Two-column layout for PRNU visualization
        col1, col2 = st.columns(2)

        with col1:
            st.markdown("##### Real Image PRNU")
            st.image(img4, use_column_width=True, caption="Real Images — PRNU Noise Maps")

        with col2:
            st.markdown("##### Fake Image PRNU")
            st.image(img5, use_column_width=True, caption="Fake Images — PRNU Noise Maps")

        # Bullet point differences
        st.markdown("**Key Differences Between Real and Fake PRNU**")

        st.markdown("""
        - **Noise Pattern Consistency:**  Real images show stable and continuous PRNU noise across the entire face and background, while fake images 
        exhibit patchy or inconsistent noise, with regions that appear noisy and others unnaturally flat.

        - **High-Frequency Texture Detail:**  Real images retain fine-grained high-frequency textures such as skin pores, hair, and edge details, whereas 
        fake images look smoother, producing irregular PRNU maps due to generative interpolation.

        - **Edge Alignment and Structural Integrity:**  Real PRNU maps highlight natural edges that align with the underlying facial structure, while fake PRNU maps 
        often contain speckle noise or artifacts unrelated to the image content, reflecting GAN upsampling behavior.

        - **Noise Fingerprint Stability:**  Real images maintain a consistent PRNU fingerprint across multiple samples, whereas fake images vary 
        significantly because GAN models do not embed a persistent or coherent sensor noise pattern.
        """)

        # Conclusion
        st.markdown("""
        The comparison clearly indicates that **real images contain strong, structured, and consistent PRNU patterns**, 
        while **fake images lack authentic sensor noise**, showing unstable or artificial noise instead. This makes PRNU a valuable forensic feature for deepfake detection.
        """)
        
        st.markdown("---")
        
        st.markdown("""
        #### LBP Analysis 
        """)
        
        # --- LBP Intro ---
        st.markdown("""
        Local Binary Patterns (LBP) capture fine-grained texture information by comparing each pixel 
        to its surrounding neighbourhood.  
        The visualizations below compare **real vs fake LBP maps**, revealing differences in micro-texture, 
        edge consistency, and generative artifacts.
        """)

        # --- Two-column layout for LBP visualization ---
        col1, col2 = st.columns(2)

        with col1:
            st.markdown("##### Real Image LBP")
            st.image(img6, use_column_width=True, caption="Real Images — LBP Noise Maps")

        with col2:
            st.markdown("##### Fake Image LBP")
            st.image(img7, use_column_width=True, caption="Fake Images — LBP Texture Maps")

        # --- Bullet point differences ---
        st.markdown("**Key Differences Between Real and Fake LBP**")

        st.markdown("""
        - **Texture Consistency:**  Real images produce structured and coherent LBP texture patterns, while fake images show irregular or chaotic LBP maps caused by synthetic texture generation.

        - **Micro-Texture Detail:**  Real images preserve fine-grained details such as skin pores, wrinkles, and edge textures.  Fake images appear smoother, leading to overly uniform or weakened LBP responses.

        - **Edge and Contour Sharpness:**  Real LBP maps highlight clear and continuous contours around facial features. Fake LBP maps often break into noisy or distorted patterns due to GAN reconstruction limitations.

        - **Texture Stability Across Samples:**  Real images maintain consistent LBP behavior across multiple samples due to natural lighting and surface structure. Fake images vary significantly between samples, reflecting unstable generative texture synthesis.
        """)

        # --- Conclusion ---
        st.markdown("""
        Overall, LBP reveals noticeable texture differences between real and synthetic faces.  
        **Real images preserve authentic micro-texture patterns**, whereas **fake images contain smoother or 
        inconsistent textures**, making LBP a valuable feature for deepfake detection in the spatial domain.
        """)
        
        st.markdown("---")
        
        st.markdown("""
        #### DCT Analysis 
        """)
        st.markdown("""
        Discrete Cosine Transform (DCT) reveals the distribution of image energy across low, mid, 
        and high-frequency components. By visualising the **2D DCT spectrum** and **radial frequency curves**, 
        we can compare how real and fake images differ in their frequency characteristics.
        """)

        # Two-column layout for DCT visualization
        col1, col2 = st.columns(2)

        with col1:
            st.markdown("##### Real Image DCT")
            st.image(img8, use_column_width=True, caption="Real Images — DCT Spectrum & Radial Frequency")

        with col2:
            st.markdown("##### Fake Image DCT")
            st.image(img9, use_column_width=True, caption="Fake Images — DCT Spectrum & Radial Frequency")

        # Bullet point differences
        st.markdown("**Key Differences Between Real and Fake DCT Patterns**")

        st.markdown("""
        - **Energy Distribution Stability:**  Real images show smooth and naturally decaying energy from low to high frequencies.  Fake images often exhibit slight irregularities, especially in the mid-to-high frequency regions.

        - **Low-Frequency Behaviour:**  Real images maintain a strong energy concentration in the low-frequency band, reflecting natural illumination and shading.  Fake images tend to have more compressed or weakened low-frequency peaks due to GAN synthesis smoothing.

        - **High-Frequency Texture Variation:**  Real images preserve richer high-frequency textures (edges, fine details), resulting in stronger high-frequency energy.  Fake images usually appear smoother, producing lower or inconsistent high-frequency values in the DCT radial curve.

        - **Spectrum Uniformity:**  Real DCT maps show more natural randomness across the frequency grid.  Fake DCT maps sometimes show subtle banding, grid patterns, or unnatural regularity caused by upsampling operations in GANs.
        """)

        # Conclusion
        st.markdown("""
        Overall, the DCT comparison demonstrates that **real images contain more natural and stable frequency distributions**, while **fake images introduce subtle distortions** in both low- and high-frequency regions. These differences make DCT a useful forensic feature for detecting synthetic image generation.
        """)

        st.markdown("---")
        
        st.markdown("""
        #### FFT Analysis 
        """)
        
        st.markdown("""
        Fast Fourier Transform (FFT) reveals how image information is distributed across different frequency ranges.  
        By converting images from the spatial domain to the frequency domain, FFT helps identify patterns that distinguish 
        **real camera images** from **GAN-generated deepfakes**.
        """)

        # Two-column layout for FFT visualization
        col1, col2 = st.columns(2)

        with col1:
            st.markdown("##### Real Image FFT")
            st.image(img10, use_column_width=True, caption="Real Images — FFT Spectrum & Radial Plot")

        with col2:
            st.markdown("##### Fake Image FFT")
            st.image(img11, use_column_width=True, caption="Fake Images — FFT Spectrum & Radial Plot")

        # Bullet point differences
        st.markdown("**Key Differences Between Real and Fake FFT Patterns**")

        st.markdown("""
        - **Frequency Distribution Consistency:**  Real images show a smooth and natural decay from low-frequency to high-frequency components, while fake images often display irregular frequency transitions due to generative model artifacts.

        - **Low-Frequency Energy Shape:**   Real FFT spectra have a clean centralized low-frequency peak, whereas fake images typically introduce cross-shaped or grid-like patterns caused by GAN upsampling filters.

        - **High-Frequency Noise Behavior:**  Real images maintain fine, consistent high-frequency textures linked to real sensor capture. Fake images often produce excessive or imbalanced high-frequency noise because GANs synthesize textures artificially.

        - **Radial Spectrum Slope Stability:**  Real images show stable and predictable radial spectrum curves, while fake images exhibit distortions, bumps, or flatter slopes that reflect synthetic texture generation rather than natural imaging.
        """)

        # Conclusion
        st.markdown("""
        Overall, the FFT analysis shows that **real images contain smooth, natural, and consistent frequency distributions**, while **fake images reveal artificial frequency structures**, including cross artifacts, abnormal decay patterns, and irregular high-frequency noise. These differences make FFT a powerful signal-level feature for deepfake detection.
        """)


